import React from 'react';
import ReactDOM from 'react-dom/client';
import PofoComponent from './conponents/PofoComponent';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PofoComponent />
  </React.StrictMode>
);
